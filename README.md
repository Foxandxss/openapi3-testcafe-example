This example is not runnable for people outside the course, so the URL and credentials are missing (just to prevent data destruction)

To run, fill the `.env` file with provided data and then:

```
$ npm t
```